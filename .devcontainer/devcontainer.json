{
  "name": "ZMK Build Environment",
  "image": "zmkfirmware/zmk-build-arm:stable",
  "postCreateCommand": "if [ ! -d .west ]; then west init -l config; fi && west update && west zephyr-export",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools"
      ],
      "settings": {
        "C_Cpp.default.intelliSenseMode": "linux-gcc-arm"
      }
    }
  }
}
